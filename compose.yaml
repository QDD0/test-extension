services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB='
      - 'POSTGRES_PASSWORD='
      - 'POSTGRES_USER='
    ports:
      - '5439:5432'

volumes:
  
  postgres_data: